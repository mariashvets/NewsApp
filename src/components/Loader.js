import React from 'react';
import PropTypes from 'prop-types';

function Loader() {
    return (
        <h1>Loading...</h1>
    );
}

Loader.propTypes = {};
Loader.defaultProps = {};

export default Loader;
